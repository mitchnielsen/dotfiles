#!/usr/bin/env bash
# Wrapper around delta that enables side-by-side diffs when the terminal is
# wide enough.

THRESHOLD=160
WIDTH="${COLUMNS:-$(tput cols 2>/dev/null || echo 80)}"

args=(--dark)
[ "$WIDTH" -ge "$THRESHOLD" ] && args+=(--side-by-side)

exec delta "${args[@]}" "$@"
