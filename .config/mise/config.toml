[tools]
aws = 'latest'
actionlint = '1.7.1'
golang = 'system'
helm = 'system'
istioctl = "latest"
rust = 'latest' # for uvx
terraform = "1.10.5"
uv = 'latest' # for python
python = "latest"

[settings]
experimental = true

# config files with these prefixes will be trusted by default
trusted_config_paths = [
  '~/code',
]
