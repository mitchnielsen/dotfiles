---
description: Push changes to the git remote
---

Push the branch to the remote with `git push origin <branch>`.

If there are changes on the target branch that are not present locally,
run `git pull --rebase`.
