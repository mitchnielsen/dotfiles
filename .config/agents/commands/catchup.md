---
description: Catch up with the current changes
---

Read all the changed files in the current git branch.
If a pull request exists, look at the title, description, and any comments.

View the diff using `gh pr diff`.

Consider the following additional context for these changes: $ARGUMENTS

Do your best to understand what the goal is, what the current changes are,
and what still needs to be done (if anything) to achieve the goal.
