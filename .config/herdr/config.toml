# herdr configuration
# https://herdr.dev/docs/configuration

onboarding = false

[theme]
# Use the terminal theme so Herdr follows Ghostty's clarity-light/dark ANSI palette.
name = "terminal"

# Clarity overrides on top of the terminal palette.
auto_switch = false
[theme.custom]
panel_bg = "reset"
accent = "blue"
blue = "blue"
green = "green"
red = "red"
yellow = "yellow"

[keys]
# Accepted key syntax: plain keys, ctrl/shift/alt/cmd/super modifiers, and special keys like enter/tab/esc/left/right/up/down.
# Named punctuation such as minus, comma, percent, double_quote, ampersand, plus, and backtick is also accepted.
prefix = "ctrl+b"
help = "prefix+?"
settings = "prefix+;"
detach = "prefix+d"
reload_config = "prefix+r"
copy_mode = "prefix+["
edit_scrollback = ["prefix+e", "prefix+ctrl+e"]

# Workspaces
workspace_picker = ["prefix+s", "cmd+o"]
goto = "prefix+ctrl+g"
new_workspace = "prefix+shift+n"
rename_workspace = "prefix+$"
close_workspace = "prefix+shift+d"
previous_workspace = "cmd+shift+["
next_workspace = "cmd+shift+]"

# Tabs
new_tab = "prefix+c"
rename_tab = "prefix+,"
previous_tab = "prefix+p"
next_tab = "prefix+n"
switch_tab = "prefix+1..9"
close_tab = ["prefix+shift+x"]

# Panes
rename_pane = "prefix+shift+p"
focus_pane_left = "prefix+h"
focus_pane_down = "prefix+j"
focus_pane_up = "prefix+k"
focus_pane_right = "prefix+l"
swap_pane_left = "prefix+shift+h"
swap_pane_down = "prefix+shift+j"
swap_pane_up = "prefix+shift+k"
swap_pane_right = "prefix+shift+l"
cycle_pane_previous = "prefix+shift+tab"
split_vertical = ["cmd+s", "prefix+v", "prefix+percent"]
split_horizontal = ["cmd+i", "prefix+minus", "prefix+double_quote"]
close_pane = ["prefix+x", "cmd+x", "cmd+w"]
zoom = "prefix+z"
resize_mode = "prefix+shift+r"
toggle_sidebar = "cmd+b"

[[keys.command]]
key = "prefix+g"
type = "pane"
command = "lazygit"

[[keys.command]]
key = "prefix+o"
type = "pane"
command = "zsh -lc 'cd /Users/mitch/code/github.com/mitchnielsen/notes && nvim .'"

[ui]
prompt_new_tab_name = true
prompt_new_workspace_name = true
agent_panel_sort = "spaces"
sidebar_collapsed_mode = "hidden"

[ui.sidebar.spaces]
row_gap = 1
rows = [
  ["workspace"],
]

[ui.sidebar.agents]
row_gap = 1
rows = [
  ["workspace", "tab"],
  ["state_icon", "state_text", "agent"]
]

[ui.sound]
enabled = false

[session]
resume_agents_on_restore = true
