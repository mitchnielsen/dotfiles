# dotfiles

Forever-changing dotfiles for tools I use.

## Installation

Run the following commands to set up the development environment:

```shell
git clone git@github.com:mitchnielsen/dotfiles.git ~/dotfiles
cd ~/dotfiles
mise run setup
```

See `mise tasks` for available maintenance tasks.
